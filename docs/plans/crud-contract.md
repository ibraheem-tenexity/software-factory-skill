# Consolidated CRUD contract (no mock data; full CRUD everywhere)

Operator directive: **no dummy/hardcoded data anywhere; full CRUD APIs for every entity.** This is
the single reference for both frontends (mwbcupd9 customer product · mhk7nz7i Tenexity OS). Branch
`worktree-tenexity-os-api` off main `daf5878`. `[EXISTS]` = already shipped; `[NEW]` = this CRUD pass.

> Auth legend: **owner** = run owner or admin (`authorize_run`); **member** = has an org on file;
> **org-admin** = role `admin`; **staff** = `require_staff` (service-token ∪ (role `admin` AND `is_internal`)).
> Rebases onto the run→project rename (`run_id`→`project_id`, `/api/runs`→`/api/projects`); shapes stable.

## Mock-data elimination (2 sources)
- `/api/admin/tools` served a curated **code constant** → **[NEW]** backed by a real `mcp_tools` table,
  seeded with the real tools at boot, fully CRUD-able. No hardcoded list remains.
- `/api/admin/agents` roster identity was a **code constant** → **[NEW]** backed by a seeded
  `agent_registry` table (CRUD); live cost/success/active still merged from `public.agents`.

## Projects (runs)
| Op | Route | Auth | Note |
|---|---|---|---|
| C | `POST /api/runs`, `POST /api/drafts` | member | [EXISTS] |
| R | `GET /api/runs`, `GET /api/runs/{id}` (+ overview/documents/tickets/deployments/brief/evidence/graph/events/artifact/log/deps) | owner | [EXISTS] |
| U | `PATCH /api/runs/{id}` `{name?, description?}` | owner | **[NEW]** rename a promoted project (draft uses `PATCH /api/runs/{id}/draft`) |
| U | `PATCH /api/admin/projects/{id}` `{is_demo}` | staff | [EXISTS] |
| D | `DELETE /api/runs/{id}` | owner | **[NEW]** archive (soft: `RunState.archived=true`, hidden from lists); discards a draft outright |

## Orgs / clients (tenants)
| Op | Route | Auth | Note |
|---|---|---|---|
| C | `POST /api/org` (self) · `POST /api/admin/clients` `{name, …}` | member / staff | self [EXISTS]; **[NEW]** admin create |
| R | `GET /api/org` · `GET /api/admin/clients` | member / staff | [EXISTS] |
| U | `PATCH /api/org`, `PATCH /api/org/billing` · `PATCH /api/admin/clients/{org_id}` | org-admin / staff | self [EXISTS]; **[NEW]** admin update by id |
| D | `DELETE /api/admin/clients/{org_id}` | staff | **[NEW]** (unlinks members, removes org) |

## Org members  ·  [EXISTS, complete]
`GET/POST/PATCH/DELETE /api/org/members[/{email}]` (org-admin).

## KB docs
| Op | Route | Auth | Note |
|---|---|---|---|
| C/R/D | `POST/GET /api/org/docs`, `DELETE /api/org/docs/{id}`, `POST /api/org/docs/{id}/use` | org-admin/member | [EXISTS] |
| U | `PATCH /api/org/docs/{id}` `{name?, tag?}` | org-admin | **[NEW]** rename / retag |

## Agents + editable prompts
| Op | Route | Auth | Note |
|---|---|---|---|
| R | `GET /api/admin/agents`, `GET /api/admin/agents/{callsign}` | staff | [EXISTS] (now from `agent_registry` + live rollups) |
| C | `POST /api/admin/agents` `{callsign,name,role,model,desc,cost_tier}` | staff | **[NEW]** |
| U | `PATCH /api/admin/agents/{callsign}` `{name?,model?,desc?,…}` · `PATCH /api/admin/agents/{callsign}/prompt` `{prompt}` | staff | identity **[NEW]**; prompt [EXISTS] (`applied:false` — saved, not yet wired to live pipeline) |
| D | `DELETE /api/admin/agents/{callsign}` | staff | **[NEW]** |

## Tools / MCP registry  (all from `mcp_tools` table)
| Op | Route | Auth | Note |
|---|---|---|---|
| R | `GET /api/admin/tools` | staff | **[NEW]** from DB (was curated) |
| C | `POST /api/admin/tools` `{name,type,provider,scope,auth}` | staff | **[NEW]** register |
| U | `PATCH /api/admin/tools/{id}` `{status?,…}` | staff | **[NEW]** connect/disconnect/edit |
| D | `DELETE /api/admin/tools/{id}` | staff | **[NEW]** remove |

## Invites / allow-list
| Op | Route | Auth | Note |
|---|---|---|---|
| C/R | `POST/GET /api/admin/access` | staff | [EXISTS] (`invited`→`active` on first login) |
| U | `PATCH /api/admin/access/{email}` `{role?, status?}` | staff | **[NEW]** change role / re-invite |
| D | `DELETE /api/admin/access/{email}` | staff | **[NEW]** revoke (removes from allow-list) |

## Drafts  ·  [EXISTS]
`POST /api/drafts`, `GET /api/runs/{id}/brief`, `PATCH /api/runs/{id}/draft`, `POST /api/runs/{id}/attach`,
`POST /api/runs/{id}/promote`; discard = `DELETE /api/runs/{id}`.

## Deployments · Tickets  ·  READ-ONLY BY DESIGN
`GET /api/runs/{id}/deployments`, `GET /api/runs/{id}/tickets` — **system-generated by the pipeline**,
not operator-writable (write CRUD here would let operators fabricate pipeline output). Reads are real
datastore. (Confirmed intentional with the coordinator.)
