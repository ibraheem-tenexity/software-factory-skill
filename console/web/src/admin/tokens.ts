// Re-exports the shared token module (SOF-11 — this used to be a byte-for-byte duplicate of
// onboarding/design.tsx's `T`; both now share one source in ../theme).
export { T, type TColor } from "../theme";
