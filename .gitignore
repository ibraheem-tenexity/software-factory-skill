__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
.runs/
.runs-dev/
.projects/
.venv/
*.db
.env
env_text.txt
open-ai-key.txt
client_secret_*.json
# Dependency lockfile for a package manager (uv) the Dockerfile build doesn't use — the pip
# install reads pyproject.toml directly (SOF-48); an uv.lock here is stray local tooling output.
uv.lock

# Design prototype reference for Task 4 (do not commit) — see .design-source/NOTES.md
.design-source/
console/web/node_modules

# Playwright MCP session dumps (browser console logs + page snapshots) — generated, never commit
.playwright-mcp/

# claude-flow — not used; never commit any of it
.claude-flow/

# AGENTS.md is a local symlink to CLAUDE.md so Codex reads the same instructions as Claude Code.
# It is intentionally NOT tracked — recreate it after cloning if you use Codex (see README).
AGENTS.md
